name: Quality
on: [push]

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ['1.19', '1.20', '1.21.x']
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-go@v4
      with:
        dotnet-version: ${{ matrix.go-version }}
    - name: Install dependencies
      run: go get .
    - name: Test
      run: go test
    - name: Build
      run: go build
